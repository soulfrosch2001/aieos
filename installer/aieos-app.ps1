# AIEOS app launcher (the clickable "AIEOS" program). Opens the native "Navy Aurora" window
# — a real Windows program (WPF), no browser, no terminal, no local server.
& (Join-Path $PSScriptRoot 'aieos-launcher.ps1')
