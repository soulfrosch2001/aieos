# Forge Sandboxes

Temporary workspace roots for the `sandbox` autonomy profile. Each run receives a new
directory and removes it on exit unless `FORGE_PRESERVE_SANDBOX=on` is set. Host command
execution still requires an explicitly configured hardened external runner.
