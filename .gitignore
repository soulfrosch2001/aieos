# Node tooling (audio generator, etc.) — installed locally, never committed.
node_modules/
npm-debug.log*

# Generated working files
*.log

# Forge runtime artifacts (run traces + agent scratch workspaces)
forge/runs/*.json
forge/examples/*/workspace/
